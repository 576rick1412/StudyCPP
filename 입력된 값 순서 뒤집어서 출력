#include <iostream>
#include <stack>

using namespace std;

int main()
{
	const int index = 6;
	int temp;
	stack<int>st;

	for (int i = 0; i < index; i++)
	{
		cin >> temp;
		st.push(temp);
	}

	while (!st.empty())
	{
		cout << st.top() << " ";
		st.pop();
	}
	
	return 0;
}
